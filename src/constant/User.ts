export type UserProfileDTO = {
    name: string 
    email: string
    phone: string
    address: string
    quota: number
    referral: string
}

export type EditUserDTO = {
    name: string 
    email: string
    phone: string
    address: string
}